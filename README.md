# Numeric-Prediction-HRDataset
For the numeric prediction problem, we predict PayRate for the employees. 
We are comparing outputs and RMSE(Root mean squared error) of three different models: 
Lasso
Decision Tree
Random Forest
